---
name: hal-hybrid-model-workflow
description: HAL 分层混合模型工作流 — 强模型负责理解/拆解/验收，低成本模型负责检索/提取/清洗。包含路由决策、执行层 prompt 模板、Reviewer 模板、Hermes 配置方法。用户自行选择模型和填写 API Token。
version: 5.0.0
author: HAL / F
license: MIT
tags: [hybrid-model, routing, workflow, deepseek, claude, cost-optimization, chinese]
metadata:
  hermes:
    tags: [hybrid-model, routing, workflow, deepseek, claude, cost-optimization]
    homepage: https://github.com/ph4ble/hybrid-model-workflow
---

# HAL 分层混合模型工作流 (V5)

强模型 + 低成本模型分层协作方案。从 V1-V5 五次迭代，适用于 Hermes Agent。

## 设计理念

不是所有任务都值得用最贵的模型。这套工作流把任务按复杂度分层：
- **强模型**：负责理解、拆解、判断、最终校验、对外交付
- **低成本模型**：负责检索、提取、清洗、摘要、分类，产出中间件

核心原则：**低成本模型只产出中间件，不主导最终结论；强模型对所有交付结果负责。**

---

## 适用场景

- 需要在成本和质量之间取得平衡
- 批量/重复性任务（词库维护、文档提取、资料整理）
- 需要强模型最终校验确保准确性的任务
- 任何需要"先处理再输出"的多步骤任务

## 推荐模型选型

| 角色 | 推荐选项 | 说明 |
|------|----------|------|
| 强模型（日常） | Claude Sonnet 4 / GPT-4o | 任务拆解、校验、日常复杂任务 |
| 强模型（重要） | Claude Opus 4 / GPT-4.5 | 高风险判断、架构设计、方法论 |
| 执行层 | DeepSeek V3.1 / Gemini Flash / GPT-4o-mini | 检索、提取、清洗、摘要、分类 |

> 用户根据自己的 API 供应商和预算自行选择模型。

---

## 核心原则

1. **诚实第一**：不知道就说不知道
2. **证据优先**：结论必须能回指到证据
3. **分层执行**：简单任务不浪费强模型，复杂任务不放手给弱模型
4. **最终负责制**：交付给用户的内容，强模型必须为准确性负责
5. **透明运行**：每次回答后标注使用的模型和 token 消耗

---

## ⚙️ Hermes 配置方法

### 第一步：添加低成本模型 Provider

在 `~/.hermes/config.yaml` 的 `custom_providers` 部分添加：

```yaml
custom_providers:
  - name: my-cheap-model           # 自定义名称
    base_url: https://YOUR_API_ENDPOINT/v1   # 替换为你的 API 地址
    api_key: YOUR_API_KEY_HERE               # 替换为你的 Token
    api_mode: chat_completions
    model: YOUR_CHEAP_MODEL_NAME             # 例如：DeepSeek-V3.1、gemini-2.0-flash
```

### 第二步：启用 Smart Model Routing（可选）

```yaml
smart_model_routing:
  enabled: true
  max_simple_chars: 160    # 消息字符数阈值
  max_simple_words: 28     # 消息词数阈值
  cheap_model:
    provider: my-cheap-model
    model: YOUR_CHEAP_MODEL_NAME
```

> ⚠️ 安全提醒：编辑 config.yaml 时不要用 terminal 读取后再粘贴 API key，会导致 key 被截断。直接用文本编辑器编辑，或用 Python yaml 库安全写入。

---

## 任务路由规则

收到任务后先做四个判断：
1. 任务是否清晰？
2. 任务是否高风险？
3. 能否拆成执行层子任务？
4. 最终结果是否直接交付？

### Route A：直接执行
- 条件：任务清晰 + 风险低 + 检索/提取/格式处理类
- 动作：低成本模型执行 → 强模型快速复核 → 交付

### Route B：先拆解再执行
- 条件：复合任务 + 多步骤 + 步骤间有依赖
- 动作：强模型拆解 → 分派执行层 → 汇总验收

### Route C：强模型主导
- 条件：高风险 / 高模糊 / 策略类
- 动作：强模型直接主导，可调执行层做资料预处理

### Route D：信息不足
- 条件：目标不明确 / 输入缺失
- 动作：先请求澄清

---

## 低成本执行层 Prompt 模板

调用低成本模型时，用以下模板包装指令：

```
你是一个受限执行器。严格按照指令完成信息处理工作。

## 你的身份
- 你不是最终决策者
- 你只产出中间件，不产出最终结论
- 你的输出会被强模型审核

## 硬性规则
1. 只基于给定材料工作，不引入外部知识
2. 不做价值判断、不给建议、不下结论
3. 找不到就说"Not Found"，不确定就说"Uncertain"
4. 不把推测写成事实
5. 不在信息不足时硬凑完整答案
6. 置信度只填 Low 或 Medium，不允许 High

## 当前任务
{在此填写具体任务描述}

## 允许范围
{在此填写允许处理的材料/内容范围}

## 输出格式（必须严格遵守）

### Task Type
{检索 / 提取 / 清洗 / 摘要 / 分类 / 对比 / 归纳}

### Source Scope
{本次基于哪些材料}

### Evidence Found
{实际找到的证据、片段、关键词、来源位置}

### Extracted Facts
{从证据中明确提取的事实，不写推测}

### Uncertain Points
{证据不足、无法确认、可能冲突的点}

### Missing Information
{还缺什么才能进入下一步}

### Suggested Next Step
{补检索 / 补提取 / 升级强模型 / 可以进入最终汇总}

### Confidence
{Low / Medium}
```

---

## 强模型 Reviewer Prompt 模板

强模型对执行层输出做验收时使用：

```
你现在需要对以下执行层中间件进行验收。

## 用户原始任务
{填写用户的原始需求}

## 执行层输出
{填写低成本模型的中间件输出}

## 验收维度

1. 事实一致性：中间输出是否真的来自输入证据？有没有编造、扩写、误引？
2. 任务完成度：用户真正的问题是否被回答？是否遗漏关键要求？
3. 逻辑与冲突：多个子结果之间是否矛盾？结论是否跳步？
4. 风险与边界：有没有超出证据边界的说法？语气是否过度自信？
5. 表达与可交付性：结果是否清楚、可用？是否区分了事实/推断/建议？

## 你的判定（三选一）

- **Pass**：证据足够、结论边界清晰、可以直接交付 → 生成最终答复
- **Revise**：有缺口但可补 → 明确指出缺什么，打回执行层补充
- **Take Over**：中间件质量太差或任务超出执行层能力 → 强模型直接接管
```

---

## 最终校验 Checklist

强模型交付前必过：

1. 用户原问题是否被完整回答？
2. 所有重要结论是否能回指到证据？
3. 是否有任何一句话超出证据边界？
4. 是否有遗漏的限制条件？
5. 是否有未处理的冲突？
6. 是否明确区分了事实、推断、建议？
7. 是否标出了不确定性？
8. 当前输出是否达到"可以直接交付"的标准？

只要有一项不能明确回答"是"，就不应直接交付。

---

## 结果回写规则

| 目标 | 写什么 | 不写什么 |
|------|--------|----------|
| memory | 稳定用户偏好、环境事实、长期约束 | 一次性任务过程、临时中间结果 |
| Notion | 成熟方法论、学习记录、结构化知识 | 工作底稿、未定稿内容 |
| Obsidian | 词汇分析、词根关联、学习笔记 | 临时摘要 |
| 本地缓存 | 中间清洗结果、工作底稿、临时材料 | 最终结论 |

---

## 运行信息显示

每次回答后以小字标注模型和 token 使用信息，格式如：
`ℹ️ model: claude-sonnet-4-6 | tokens: ~1.2k in / ~0.8k out`

这是备注信息，不是主要内容，保持低调显示即可。

---

## 典型适用场景

1. **英语学习资料整理**：低成本层提取单词/例句/主题 → 强模型校验词义边界和重要度
2. **数据库批量维护**：低成本层扫描异常字段、批量清洗 → 强模型抽样校验
3. **长文档总结**：低成本层切分长文提取要点 → 强模型做最终可交付总结
4. **历史会话召回**：低成本层召回旧会话并提炼 → 强模型判断哪些值得沉淀
5. **方法论设计**：强模型先主导框架 → 低成本层补充资料 → 强模型校验定稿

---

## 迭代历史

- V1：分层架构，ABCD 任务分类
- V2：小模型强约束 + 强模型校验
- V3：可执行工作流规范（执行契约、验收契约）
- V4：HAL 真实工作流蓝图（路由器、中间件、回写规则）
- V5：系统提示词草案 + Hermes 配置 + 模型选型更新 + token 显示

---

## Pitfalls

1. **yaml.dump 丢注释**：编辑 config.yaml 后确认功能正常即可
2. **API key 脱敏**：终端输出中 API key 会被截断，不要用截断值做 patch
3. **smart_model_routing 阈值**：只对消息字符/词数做简单判断，复杂任务仍需手动路由
4. **delegate_task 无上下文**：子代理没有当前对话历史，必须在 context 里传够信息
5. **低成本模型幻觉**：一定要通过执行层模板约束输出格式，不给它自由发挥的空间
6. **强模型验收 ≠ 润色**：必须做真正的事实复核和边界控制，不只是改改措辞
